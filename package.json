{
  "name": "chatgpt-the-master-app",
  "private": true,
  "version": "1.0.0",
  "description": "Static single-file web apps (My Life Master App suite). No build step -- these files are served as-is by GitHub Pages.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
